@using RouteDataManager.ViewModels
@model SeasonIndexViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<form asp-action="Index">

            <div class="form-group">
                <table class="table">
                 <thead>

                <tr>
                    <th>
                         <label class="control-label">Country</label>
                        <select asp-for="@Model.FilterCountry.CountryID" class ="form-control" asp-items="@Model.SelectListCountries"></select>
                    </th>

                    <th>
                     
                    </th>
                    

                    <th>
                      
                    </th>

                     <th>
                      
                    </th>
                    
                    <th>
                            <input type="submit" value="Search" class="btn btn-primary btn-block" />
                    </th>
                </tr>

                </thead>
                </table>

            </div>
</form>
<p>
    <a asp-action="Create" class="btn btn-primary rounded-0"><i class="fa fa-plus-circle"></i>Add New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                Month
            </th>
            <th>
                Season Description
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Month_SeasonDescription)
        {
           
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Key)
                    </td>

                    <td>
                          @Html.DisplayFor(modelItem => item.Value)
                    </td>
                                        
                </tr>
            }
        
    </tbody>
</table>

